name: Frontend Check Linting (ESLint)

on:
  pull_request:
    branches:
      - main

jobs:
  frontend-check-linting:
    runs-on: ubuntu-latest
    container:
      image: node:24-alpine
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v3
        with:
          node-version: 24
      - run: npm install
      - run: npm run lint
