name: Production Deployment

on:
  push:
    branches:
      - prod
  pull_request:
    branches:
      - dev

jobs:
  deploy-dev-stack:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/deploy.yml
    with:
      environment: production
