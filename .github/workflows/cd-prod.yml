name: Production Deployment

on:
  push:
    branches:
      - prod

jobs:
  deploy-dev-stack:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/deploy.yml
    with:
      environment: production
