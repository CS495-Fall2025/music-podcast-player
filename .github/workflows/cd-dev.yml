name: Development Deployment

#on:
#  push:
#    branches:
#      - dev
on:
  pull_request:
    branches:
      - dev

jobs:
  deploy-dev-stack:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/deploy.yml
    with:
      environment: development
